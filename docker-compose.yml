services:
  toonflow:
    build: .
    container_name: toonflow
    ports:
      - "10588:10588"
    volumes:
      - ./data:/app/data
    environment:
      NODE_ENV: dev
      PORT: 10588
    restart: unless-stopped
